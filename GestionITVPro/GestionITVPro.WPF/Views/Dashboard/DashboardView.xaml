<Page x:Class="GestionITVPro.WPF.Views.Dashboard.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Panel de Control de Vehículos"
      Background="#0F0F12">

    <Page.Resources>
        <!-- Estilo para Botones Gaming -->
        <Style x:Key="GamerActionButton" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#00F2FF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#00F2FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00F2FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00F2FF" BlurRadius="20" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Título -->
            <RowDefinition Height="Auto" /> <!-- Card Principal -->
            <RowDefinition Height="*" />    <!-- Detalles y Acciones -->
        </Grid.RowDefinitions>

        <!-- 1. CABECERA -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,30">
            <Border Width="50" Height="50" Background="#00F2FF" CornerRadius="12" Margin="0,0,15,0">
                <TextBlock Text="🚗" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel>
                <TextBlock Text="CENTRO DE CONTROL" Foreground="White" FontSize="32" FontWeight="Black"/>
                <TextBlock Text="Gestión de Flota de Vehículos" Foreground="#555" FontSize="13"/>
            </StackPanel>
        </StackPanel>

        <!-- 2. CARD DE VEHÍCULOS (GRANDE) -->
        <Border Grid.Row="1" Background="#16161D" CornerRadius="20" Padding="30" Margin="0,0,0,30" 
                BorderBrush="#333" BorderThickness="1">
            <Grid>
                <StackPanel HorizontalAlignment="Left">
                    <TextBlock Text="TOTAL DE VEHÍCULOS" Foreground="#777" FontSize="14" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalEstudiantes}" Foreground="White" FontSize="64" FontWeight="Bold">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00F2FF" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="ESTADO: ACTIVO" Foreground="#00FF88" FontSize="14" FontWeight="Bold" 
                           HorizontalAlignment="Right" VerticalAlignment="Top"/>
            </Grid>
        </Border>

        <!-- 3. SECCIÓN INFERIOR -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Resumen Operativo -->
            <Border Grid.Column="0" Background="#16161D" CornerRadius="20" Padding="25" Margin="0,0,20,0">
                <StackPanel>
                    <TextBlock Text="ESTADÍSTICAS DE FLOTA" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,25"/>
                    
                    <StackPanel Margin="0,0,0,25">
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="VEHÍCULOS REVISADOS" Foreground="#AAA" FontSize="13"/>
                            <TextBlock Text="72%" Foreground="#00F2FF" FontSize="13" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <ProgressBar Value="72" Height="8" Foreground="#00F2FF" Background="#252525" BorderThickness="0"/>
                    </StackPanel>

                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <TextBlock Text="MANTENIMIENTO PENDIENTE" Foreground="#AAA" FontSize="13"/>
                            <TextBlock Text="28%" Foreground="#FF0055" FontSize="13" HorizontalAlignment="Right" FontWeight="Bold"/>
                        </Grid>
                        <ProgressBar Value="28" Height="8" Foreground="#FF0055" Background="#252525" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Comandos -->
            <Border Grid.Column="1" Background="#1A1A23" CornerRadius="20" Padding="25">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="ACCIONES" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,0,0,25" HorizontalAlignment="Center"/>
                    
                    <Button Content="REFRESCAR LISTA 🔄" Style="{StaticResource GamerActionButton}"/>
                    <Button Content="VER GRÁFICOS 📊" Style="{StaticResource GamerActionButton}" 
                            BorderBrush="#7000FF" Foreground="#7000FF" Background="#120B20"/>
                    
                    <Border Background="#252535" CornerRadius="12" Padding="15" Margin="0,15,0,0">
                        <TextBlock Text="SISTEMA ITV LUIS VIVES V.2.0" 
                                   Foreground="#555" FontSize="11" TextAlignment="Center" FontWeight="Bold"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
