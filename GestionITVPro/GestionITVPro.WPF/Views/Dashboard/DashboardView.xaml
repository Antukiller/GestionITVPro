<Page x:Class="GestionITVPro.WPF.Views.Dashboard.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="720" d:DesignWidth="1000"
      Title="Panel de Control de Citas"
      Background="#0F0F12">

    <Page.Resources>
        <Style x:Key="GamerActionButton" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="#1A1A2E"/>
            <Setter Property="Foreground" Value="#00F2FF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#00F2FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00F2FF"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
        <Grid Margin="30" Width="950">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> <RowDefinition Height="Auto" /> </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,25">
                <Border Width="55" Height="55" Background="#00F2FF" CornerRadius="12" Margin="0,0,15,0">
                    <TextBlock Text="📊" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CENTRO DE CONTROL" Foreground="White" FontSize="32" FontWeight="Black"/>
                    <TextBlock Text="Umbrella ITV - Gestión de Datos Operativos" Foreground="#555" FontSize="12" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Row="1" Background="#16161D" CornerRadius="20" Padding="30" Margin="0,0,0,25" 
                    BorderBrush="#333" BorderThickness="1">
                <Grid>
                    <StackPanel HorizontalAlignment="Left">
                        <TextBlock Text="VOLUMEN TOTAL DE VEHÍCULOS" Foreground="#777" FontSize="13" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Informe.TotalCitas}" Foreground="White" FontSize="64" FontWeight="Black">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#00F2FF" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text=" REGISTROS" Foreground="#333" FontSize="20" FontWeight="Black" VerticalAlignment="Bottom" Margin="10,0,0,12"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top">
                        <Border Background="#0D2A1F" CornerRadius="5" Padding="8,4" Margin="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" Fill="#00FF88" Margin="0,0,8,0"/>
                                <TextBlock Text="MONITORIZACIÓN ACTIVA" Foreground="#00FF88" FontSize="11" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <TextBlock Text="{Binding Informe.UltimaCitaProgramada, StringFormat='Último dato: {0:dd/MM/yy}'}" 
                                   Foreground="#555" FontSize="10" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.6*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#16161D" CornerRadius="20" Padding="25" Margin="0,0,20,0" BorderBrush="#222" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="RESUMEN OPERATIVO" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,25"/>
                        
                        <StackPanel Margin="0,0,0,20">
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="CITAS FINALIZADAS" Foreground="#AAA" FontSize="12" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Informe.CitasCompletadas}" Foreground="#00F2FF" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding Informe.PorcentajeCompletadas, StringFormat=' ({0:F0}%)'}" Foreground="#555" FontSize="12" VerticalAlignment="Bottom"/>
                                </StackPanel>
                            </Grid>
                            <ProgressBar Value="{Binding Informe.PorcentajeCompletadas, Mode=OneWay}" Height="10" Foreground="#00F2FF" Background="#0A0A0C" BorderThickness="0"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,25">
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="PENDIENTES" Foreground="#AAA" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Informe.PorcentajePendientes, StringFormat={}{0:F0}%}" 
                                           Foreground="#FF0055" FontSize="14" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <ProgressBar Value="{Binding Informe.PorcentajePendientes, Mode=OneWay}" Height="10" Foreground="#FF0055" Background="#0A0A0C" BorderThickness="0"/>
                        </StackPanel>
                        
                        <UniformGrid Columns="3" Margin="0,15,0,0">
                            <StackPanel>
                                <TextBlock Text="PARA HOY" Foreground="#555" FontSize="11" FontWeight="Black"/>
                                <TextBlock Text="{Binding Informe.CitasParaHoy}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="ATRASADAS" Foreground="#555" FontSize="11" FontWeight="Black"/>
                                <TextBlock Text="{Binding Informe.CitasAtrasadas}" Foreground="#FF0055" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock Text="ELÉCTRICOS" Foreground="#555" FontSize="11" FontWeight="Black"/>
                                <TextBlock Text="{Binding Informe.Electrico}" Foreground="#00FF88" FontSize="24" FontWeight="Bold"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#1A1A23" CornerRadius="20" Padding="25" BorderBrush="#333" BorderThickness="1">
                    <StackPanel VerticalAlignment="Stretch">
                        <TextBlock Text="DATOS" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,25" HorizontalAlignment="Center"/>
                        
                        <Button Content="ACTUALIZAR PANEL 🔄" Command="{Binding RefrescarCommand}" Style="{StaticResource GamerActionButton}"/>
                        <Button Content="VER GRÁFICOS 📈" Command="{Binding VerGraficosCommand}" Style="{StaticResource GamerActionButton}" 
                                BorderBrush="#7000FF" Foreground="#7000FF" Background="#120B20"/>

                        <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                            <StackPanel>
                                <TextBlock Text="ESTACIÓN" Foreground="#555" FontSize="9" FontWeight="Bold" TextAlignment="Center"/>
                                <TextBlock Text="LUIS VIVES ITV" Foreground="White" FontSize="12" FontWeight="Bold" TextAlignment="Center" Margin="0,2,0,0"/>
                                <Separator Background="#333" Margin="0,10"/>
                                <TextBlock Text="ESTADO" Foreground="#555" FontSize="9" FontWeight="Bold" TextAlignment="Center"/>
                                <TextBlock Text="Sincronizado" Foreground="#00FF88" FontSize="10" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Viewbox>
</Page>